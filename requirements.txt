# Parrotlet ASR + SGLang MedGemma on RTX 6000 Pro (96GB / 48GB GPU)
#
# PyTorch with CUDA is expected in the VM base image (CUDA 12.x / 13.x).

# Machine Learning & Hugging Face Stack
transformers==5.12.1
accelerate>=1.1.0
safetensors>=0.4.0
huggingface_hub>=1.10,<2

# SGLang Engine & Low-Latency Acceleration
sglang>=0.5.19
flashinfer>=0.2.0; platform_system == "Linux"
xgrammar>=0.1.18

# Audio Processing & Resampling (apt: ffmpeg libsndfile1)
librosa>=0.10.2,<0.12
soundfile>=0.12.1
numpy>=1.26,<3
resampy>=0.4.3

# Web Server (FastAPI + Uvicorn)
fastapi==0.141.1
pydantic==2.13.5
python-multipart>=0.0.9
requests>=2.31
uvicorn>=0.27.0
httpx>=0.27.0
